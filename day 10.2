movies = ["3 idiots", "Jurriassic World" , "Housefull"]

class movie:
    def __init__(self):
        self.movies=movies
      
    def addition(self):
            name = input("Enter the name of the movie that you want to add:")
            self.movies.append(name)
            print("✅ Movie added successfully")
            self.view() 

    def view(self):
            print("Here are all the movies that you have watched till the date:")
            for movie in self.movies:
                print(f"\n🌸 {movie}")

    def remove(self):
            name = input("Enter the name of the movie that you want to remove:").lower()
            for movie in self.movies:
                if movie.lower() == name:
                    self.movies.remove(movie)
                    print("Movie removed successfully")
                    self.view()
                    break
                else:
                    print("Movie doesn't exist")
        
    def exit(self):
            print("Exiting the program")
            return False 
manager = movie()
 
while True:
    print("🎬 Movie Manager 🎬")
    print("1. Add movie")
    print("2. View watchlist")
    print("3. Remove a movie")
    print("4. Exit")

    try:
        choice = int(input("Enter your choice:"))
    except ValueError:
        print("Enter correct choice")
        continue
    
    if choice == 1:
        manager.addition()
    elif choice == 2:
        manager.view()
    elif choice ==3:
        manager.remove()
    elif choice == 4:
        manager.exit()
    else:
        print("Enter correct choice")


    
